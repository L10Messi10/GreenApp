<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pv="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             xmlns:ff="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms" Title="Products"
             x:Class="GreenApp.Activity.ProductsPage" BackgroundColor="{StaticResource BackgroundColor}">
    <ContentPage.Resources>
        <Style TargetType="Grid">
            <Setter Property="VisualStateManager.VisualStateGroups">
                <VisualStateGroupList>
                    <VisualStateGroup x:Name="CommonStates">
                        <VisualState x:Name="Normal" />
                        <VisualState x:Name="Selected">
                            <VisualState.Setters>
                                <Setter Property="BackgroundColor"
                                        Value="LightSkyBlue" />
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </Setter>
        </Style>
    </ContentPage.Resources>
    <AbsoluteLayout>
        <Grid AbsoluteLayout.LayoutBounds="0,0,1,1"
              AbsoluteLayout.LayoutFlags="SizeProportional">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Row="0" Grid.ColumnSpan="3" Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="50" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="50" />
                </Grid.ColumnDefinitions>
                <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" BackgroundColor="#D4D4D4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="50" />
                    </Grid.ColumnDefinitions>
                    <SearchBar Grid.Column="0" CancelButtonColor="{StaticResource TextColor}"
                               IsVisible="True" HorizontalOptions="Start"
                               PlaceholderColor="Gray" Placeholder="Search products. . . ."
                               Visual="Material" TextColor="{StaticResource TextColor}" x:Name="prosearh" TextChanged="Prosearh_OnTextChanged"
                               />
                    <Image Grid.Row="0" Grid.Column="1" Source="cart.png" Aspect="AspectFit"
                           HorizontalOptions="FillAndExpand">
                        <Image.GestureRecognizers>
                            <TapGestureRecognizer Tapped="TapGestureRecognizer_OnTapped" NumberOfTapsRequired="1" />
                        </Image.GestureRecognizers>
                    </Image>
                </Grid>
                <Grid Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Grid.ColumnSpan="2">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Label Grid.Row="0" Text="Products" HeightRequest="15"
                           FontSize="20" FontAttributes="Bold" />
                    <CollectionView Grid.Row="0" ItemsSource="{Binding TBL_Products}"
                                    Margin="0,25,0,0" x:Name="ListProducts" SelectionMode="Single"
                                    VerticalOptions="Fill" SelectionChanged="ListProducts_OnSelectionChanged">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Horizontal"
                                             Span="3" />
                        </CollectionView.ItemsLayout>
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0" Padding="0">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="200" />
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="150" />
                                    </Grid.ColumnDefinitions>
                                    <StackLayout Grid.Row="0" Margin="5,0,5,0" Padding="0" Grid.Column="0">
                                        <!--<ActivityIndicator IsRunning="{Binding Source={x:Reference prodimg}, Path=IsLoading}"
                                                   IsVisible="{Binding Source={x:Reference prodimg}, Path=IsLoading}"
                                                   Color="Black"/>-->
                                        <Label VerticalOptions="Center" Margin="0,5" Padding="0" FontAttributes="Bold">
                                            <Label.FormattedText>
                                                <FormattedString>
                                                    <Span Text="{Binding prod_name}" FontSize="Medium"
                                                          TextColor="{StaticResource TextColor}" />
                                                    <Span Text="-" FontSize="12" TextColor="{StaticResource TextColor}" />
                                                    <Span Text="{Binding category_name}" FontSize="12"
                                                          TextColor="{StaticResource TextColor}" />
                                                </FormattedString>
                                            </Label.FormattedText>
                                        </Label>
                                        <Frame CornerRadius="10" Padding="0" BorderColor="Gray" HasShadow="True">
                                            <ff:CachedImage x:Name="prodimg" Source="{Binding img_uri}"
                                                            HeightRequest="150" Aspect="AspectFill"
                                                            BitmapOptimizations="True"
                                                            CacheDuration="30" LoadingPlaceholder="Image section" ErrorPlaceholder="Error" />

                                        </Frame>
                                        <StackLayout Margin="50,-155,0,50" BackgroundColor="Green" Opacity="0.8">
                                            <Label VerticalOptions="End" HorizontalOptions="EndAndExpand">
                                                <Label.FormattedText>
                                                    <FormattedString>
                                                        <Span Text="{Binding prod_price}" FontSize="15"
                                                              TextColor="White" FontAttributes="Bold" />
                                                        <Span Text=" / " FontSize="15" TextColor="White" />
                                                        <Span Text="{Binding unit_desc}" FontSize="15"
                                                              TextColor="White" />
                                                    </FormattedString>
                                                </Label.FormattedText>
                                            </Label>
                                        </StackLayout>
                                        
                                        <Label x:Name="txtprodid" IsVisible="False" Text="{Binding id}" />
                                    </StackLayout>
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </Grid>
            </Grid>
        </Grid>
        <Frame AbsoluteLayout.LayoutBounds="0.9,0,30,30" BackgroundColor="Red"
               AbsoluteLayout.LayoutFlags="PositionProportional"
               HeightRequest="20" WidthRequest="20" CornerRadius="25" Margin="0" Padding="0">
            <Label Text="0" TextColor="WhiteSmoke" HorizontalOptions="Center" FontSize="15"
                   VerticalOptions="CenterAndExpand"
                   x:Name="lblcartcount">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="TapGestureRecognizer_OnTapped"/>
                </Label.GestureRecognizers>
            </Label>
            <Frame.GestureRecognizers>
                <TapGestureRecognizer NumberOfTapsRequired="1" Tapped="TapGestureRecognizer_OnTapped"/>
            </Frame.GestureRecognizers>
        </Frame>
    </AbsoluteLayout>
</ContentPage>